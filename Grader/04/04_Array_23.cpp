#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(0);cin.tie(0);cout.tie(0);
    int n;
    cin >> n;
    string A[n][2];
    for (int i = 0; i < n; i++)
    {
        string tmp;
        getline(cin,tmp);
        A[i][0] 
    }
    
    return 0;
}